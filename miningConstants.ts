/** Target time between blocks in seconds (matches backend TARGET_BLOCK_TIME = 600). 10min/block */
export const TARGET_BLOCK_TIME_SECONDS = 600;

/** Number of blocks per difficulty adjustment epoch (matches backend BLOCKS_PER_EPOCH = 30). */
export const BLOCKS_PER_EPOCH = 30;
